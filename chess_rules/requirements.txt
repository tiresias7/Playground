# Recovering hidden chess rules from observations.
# The container is ephemeral; install before running the experiment:
#   pip install -r chess_rules/requirements.txt
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
chess>=1.10        # ground-truth legality / coords, used ONLY for data + scoring
pytest>=7.0        # tests only
